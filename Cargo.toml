[package]
name = "rulec"
version = "0.20.0"
description = "A little language for business rules: each rule is proved free of gaps and contradictions before it compiles to plain code in twelve languages."
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
